# Motion OS Ultimate

One After Effects panel. Not a separate “Vero Motion” SKU.

**Promise:** Art from Figma. Motion from Evotechly. Finish in one panel.

## Layers

| Layer | Name | Status |
|---|---|---|
| L0 | Compiler — Scan → Apply, roles, brand JSON, shots | shipped |
| L1 | UI Kit — In / Out / Both + UI layout presets (own code) | shipped |
| L2 | Lockup / Brand — own pins + type metrics + Scan→Apply guides | shipped |
| L3 | Taste packs | shipped |
| L3.1 | SaaS shot pack v2 + Reel grammar | shipped |
| L3.2 | Editor Kit — Polish + Person + captions + recipes | shipped (v0.7) |
| L3.3 | Auto-Animate (selection, keyframe) + asset browser lite | this PR (v0.8) |
| L4 | Copilot — prompt → editable role plan only | stub |

Default style stays **stripe**. Stripe hero fixtures stay put.

## L3.3 Auto-Animate + assets lite (v0.8)

Selection Auto-Animate (Keyframe Mode): In / Out / Both, 8-way + Scale, Apple / Soft / Expo / Spring, sequence by index or Y. No role names required. Does not replace Scan → Apply.

Asset browser lists local caption templates + SRT sample paths. No CDN.

TFM Liquid Glass / Comp Exporter are companions (official sites). Not vendored. Expression In/Out is v0.9.

## No third-party binary

MIT Evotechly code only. Reminder row + official URLs only.
