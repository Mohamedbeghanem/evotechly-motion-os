# Sample SRT

Basic SubRip only: index, `HH:MM:SS,mmm --> HH:MM:SS,mmm`, text.

Limits of the panel importer:

- No ASS / styled SRT
- No overlapping cue merge
- Two-line cues stay one text layer (line break kept)
- Arabic must stay as text — do not convert these files to PNG
