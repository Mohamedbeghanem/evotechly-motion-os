# Automation Engine — v0.29

Declarative WHEN / IF / DO. Commands go through existing engines.

AUTO: read-only + marked safe writes. ASK: pending. SUGGEST: Action Center. DESTRUCTIVE: never unattended.

Default: OFF. Motion OS stays correct with P20 off.
