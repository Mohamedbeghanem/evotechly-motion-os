# Asset Engine — v0.24

Register AE layers / comps. Classify with semantic keys. Resolve by context. Never silent-swap.

Explicit binding beats locale/format rank. STRICT missing ≠ pick a random file. ALLOW_* fallbacks return `ADAPTATION REQUIRED`.

Rejected assets are excluded. Filename tokens are suggestions until confirmed.
