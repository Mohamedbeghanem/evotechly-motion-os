# Variant Engine — v0.23

Master is source of truth. ANALYZE ADAPTATION is read-only. BUILD VARIANT writes `EVO_VARIANT_*` metadata on the master store. It does not remap master cameras, type, or SFX.
