# Workspace — v0.28

HOME aggregates P15/P16/P12 counts. NEXT ACTION routes. SEARCH is a keyword index, not a project rescan.

Unmanaged comps are valid. Animate Selection does not need a Content ID.

Ambiguous context is not auto-picked.
