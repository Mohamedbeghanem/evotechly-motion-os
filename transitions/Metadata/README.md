# Metadata

`catalog.json` is the pairing surface for the panel and for Phase 18 AI.

Every row: `{ id, category, style, duration, intensity, targetRequired, bestUse, aspectRatios, sfx, phase, implemented }`.

Regenerate from `scripts/write-transition-catalog.js` if you add IDs — then update family READMEs.
