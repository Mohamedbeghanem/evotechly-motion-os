# Engine

Node source of truth: `core/transitions/`.

- `easing.js` `timing.js` `target.js` `control.js` `engine.js` `registry.js`
- JSX (`ae/Evotechly Transitions.jsx`) mirrors UI Push family numbers. ExtendScript cannot require Node.

No `.ffx` / `.aep` here. AE cannot run in CI.
