# Expressions

Reserved. Phase 1 bakes keyframes.

Later: bind `EVOTECHLY_TRANSITION_CONTROL` → Progress (0–100) so editors can time-remap a transition without re-keying.

Do not vendor third-party expression packs.
